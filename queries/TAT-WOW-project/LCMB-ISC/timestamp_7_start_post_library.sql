-- Sequencescape

-- Creation of the LB Lib PCR-XP plate:
-- table 'labware', 'created_at' field - linked to 'plate_purposes' table, where 'name' field is 'LB Lib PCR-XP'.

-- Also need to identify the correct set of samples, or at least a super set that can then join in Tableau to the MLWH datasets.

